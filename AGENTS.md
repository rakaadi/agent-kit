# Agent Kit

Agent Kit packages reusable skills for coding agents. The root `plugin.json` is the portable Agent Plugins manifest, and `skills/` contains its Agent Skills.

`agents/` and `instructions/` are authoring resources for client-specific agents and repository instruction templates. Keep them separate from portable plugin content.
